node('xcode72&&unity545'){
  def BUILD_DIR
  def OTA_WEB_URL
  stage('Checkout') {

    //proj source code
    dir('ota') {
      git 'https://github.com/xclouder/jenkins_pipeline_u3d_demo_proj.git'
    }

    //ios ota tools
    // dir('ota') {
    //   git 'https://github.com/xclouder/jenkins_ota_scripts.git'
    // }

    BUILD_DIR = "build"
    $OTA_WEB_URL = "ota.ezfun.cn"
  }

  stage('Build') {
    


  }
  stage('Archive') {
    archive 'build/*.ipa, build/*.png, build/*.plist'
  }
}